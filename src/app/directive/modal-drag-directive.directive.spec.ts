/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModalDragDirectiveDirective } from './modal-drag-directive.directive';

describe('ModalDragDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalDragDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
